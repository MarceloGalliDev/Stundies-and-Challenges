Created by Galli, Marcelo L.

#PyAudio
Usado para detecção e saída de audio da aplicação
-Necessário na aplicação de saída de audio
-Necessário o download do arquivo PyAudio
pip install PyAudio-0.2.11-cp37-cp37m-win_amd64.whl


#webcam.py
Usado na ativação do hardware de captura de vídeo, podendo ser rodada por qualquer dispositivo que possua camêra e tenha capacidade de ser acessada
-Necessário a instalação dos pacotes
pip install numpy
pip install matplotlib
pip install opencv-python


#detectandorosto.py
Usado na ativação do hardware de captura de vídeo juntamente com a detecção de face, em ambiente escuro necessário adicionar configurações
-Necessário a instalação dos pacotes
-Necessário o download da pasta cascade, usado para compor a 'fallow' da caixa de busca de face 
pip install numpy
pip install matplotlib
pip install opencv-python
